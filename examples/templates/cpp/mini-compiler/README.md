# C++ Mini-Compiler Template

This C++17 starter uses `std::variant` AST nodes, generated parser reducer
hooks, a small instruction stream, and a mock stack runtime. It is meant to be
copied and renamed when starting a new DSL.

```sh
make run
make test
```
